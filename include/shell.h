#ifndef SHELL_H
#define SHELL_H

/* Runs the kernel shell forever: reads lines from the keyboard, echoes
 * them, and dispatches a small set of built-in commands. Never returns. */
void shell_run(void);

#endif
